spring:
  application:
    name: SEBM
#  profiles:
#    active: ${SPRING_PROFILES_ACTIVE:local}

  datasource:
    url: jdbc:mysql://trolley.proxy.rlwy.net:29578/railway?useSSL=false&serverTimezone=UTC
    username: root
    password: RxAHOOQJjTtBblTpadijgyIWmArpvibI
    driver-class-name: com.mysql.cj.jdbc.Driver
  cache:
    type: redis
  data:
    redis:
      host: caboose.proxy.rlwy.net
      port: 13462
      username: default
      password: ekseulHMMJJMoAFIOLchTXsnRDbpMHff
server:
  port: 29578
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'api'
      paths-to-match: /**
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
tencent:
  cos:
    secretId: ${TENCENT_SECRET_ID:default}
    secretKey: ${TENCENT_SECRET_KEY:default}
    region: ${TENCENT_REGION:default}
    bucketName: sebm-1354340785
    durationSeconds: 900
